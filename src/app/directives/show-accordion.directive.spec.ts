import { ShowAccordionDirective } from "./show-accordion.directive";

describe('ShowAccordionDirectiveDirective', () => {
    // it('should create an instance', () => {
    //     const directive = new ShowAccordionDirective();
    //     expect(directive).toBeTruthy();
    // });
});
